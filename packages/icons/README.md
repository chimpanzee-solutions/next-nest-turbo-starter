# `@repo/icons`

Placeholder package for shared icons.

The package intentionally exports nothing right now so the monorepo structure stays intact and future projects can add icons later without changing workspace wiring.
